GEMINI_API_KEY=
EXA_API_KEY=
ELEVENLABS_API_KEY=
WISPR_API_KEY=
# Required — long random secret used to sign JWTs (e.g. openssl rand -hex 32). App will not start if empty.
JWT_SECRET=
# Paste Neon pooled connection string from neon.tech console. Never commit real secrets.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
CORS_ORIGINS=["http://localhost:3000"]
UPLOAD_DIR=./uploads
