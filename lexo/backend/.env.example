GEMINI_API_KEY=
# Optional for analyze — without it, citations are unverified / general principle (pipeline still completes).
EXA_API_KEY=
# Required for POST /api/voice/speak (TTS). Without it, speak returns 503.
ELEVENLABS_API_KEY=
# Optional — defaults to ElevenLabs "Rachel" if unset.
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
# Unused — STT is browser Web Speech API (TKT-027). Leave blank; not required to run the app.
WISPR_API_KEY=
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
JWT_SECRET=
UPLOAD_DIR=./uploads
CORS_ORIGINS=["http://localhost:3000"]
